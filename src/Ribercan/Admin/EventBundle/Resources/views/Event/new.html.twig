{% extends '::admin-base.html.twig' %}

{% block header_javascripts %}
  <script src="/assets/vendor/trix/dist/trix.js"></script>
{% endblock %}

{% block stylesheets %}
  <link rel="stylesheet" type="text/css" href="/assets/vendor/trix/dist/trix.css">
{% endblock %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block section_title %}Añadir un evento{% endblock %}

{% block body -%}
  {{ form_start(form) }}
    <section class="event-information">
      <header>
        <h2>Información relevante</h2>
      </header>
      <div>
        <div class="row">
          <div class="col-lg-12">
            {{ form_label(form.title) }}
            {{ form_widget(form.title) }}
          </div>
        </div>
        <div class="row">
          <div class="col-lg-12">
            {{ form_label(form.summary) }}
            {{ form_widget(form.summary) }}
          </div>
        </div>
        <div class="row">
          <div class="col-lg-12">
            {{ form_label(form.body) }}
            <div class="hide">{{ form_widget(form.body) }}</div>
            <trix-editor input="event_body"></trix-editor>
          </div>
        </div>
      </div>
    </section>

    <section class="event-media">
      <header>
        <h2>Imágenes</h2>
      </header>
      <div>
        <div class="row">
          <div class="col-lg-12">
            {{ form_label(form.uploadedImage) }}
            {{ form_widget(form.uploadedImage) }}
          </div>
        </div>
      </div>
    </section>

    <div class="pull-right">
      <input type="submit" class="btn btn-primary" value="Publish" />
      <a href="{{ path('admin_events_index') }}" class="btn btn-warning">Cancelar</a>
    </div>
  {{ form_end(form) }}
{% endblock %}
